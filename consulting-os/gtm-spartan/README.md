# GTM Spartan

The organization's **how we find work** function. Three stations, one loop: **hunt → list → apply**. Specialist agents on the spokes. The coordinator at the hub. A person gates anything that becomes a name, a list, or a send.

Vendor APIs only. No marketplace scrape. No cookies. No auto-submit. The OS scores and drafts. A person still clicks.

## How the loop works

**Hunt** finds the pain shape. Cards and patterns. Why this fits Audit → Install → Operate. No person names. The principal marks keep or skip.

**List** runs only on keeps. Candidate prep in one live working file.

**Apply** drafts against a listing. First line is their pain, in their words. Then what you'd install, how the method runs, a short ask. The draft sits in a queue. Submit is a human action.

## Architecture

The same interface sits behind every board and every source. Swap the adapter, keep the loop. Scoring, HITL, never-submit — those don't change when the source does.

- [Adapter contract](adapter.md) — hunt, list, apply as interfaces
- [ICP](icp.md) — the scoring policy
- [Draft shape](draft-shape.md) — what a Spartan application looks like
- [A run](canned-run.md) — the loop on a sample week
